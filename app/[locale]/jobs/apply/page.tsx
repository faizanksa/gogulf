/**
 * /<locale>/jobs/apply — the application form in another language. One implementation,
 * in app/(marketing)/jobs/apply/page.tsx. The submission carries the page's language
 * (X-Form-Locale), so the server's replies match it.
 */
export { default, generateMetadata } from "@/app/(marketing)/jobs/apply/page";
