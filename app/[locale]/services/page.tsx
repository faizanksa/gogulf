/** /<locale>/services — the service catalogue in another language. One implementation, in app/(marketing)/services/page.tsx. */
export { default, generateMetadata } from "@/app/(marketing)/services/page";
